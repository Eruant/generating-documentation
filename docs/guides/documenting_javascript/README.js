Ext.data.JsonP.documenting_javascript({"guide":"<h1>Documenting your JavaScript</h1>\n\n<p>To generate the API documentation you need to add code hints to your JavaScript files. The aim of this guide is to show you the most common and useful options.</p>\n\n<p>All of the options you set should be added within comments, so that it does not affect your JavaScript</p>\n\n<h2>Syntax</h2>\n\n<h3>Class</h3>\n\n<p>The main option you will use is <code>@class</code>. You can use it to document the name of the class.</p>\n\n<pre><code>/**\n * @class Person\n * A human being in code form\n */\n</code></pre>\n\n<p>If you are extending another class you could also use <code>@extend &lt;original classname&gt;</code>.</p>\n\n<h3>Author</h3>\n\n<p>The Author tag documents who wrote the code.</p>\n\n<pre><code>/**\n * @author Matt Gale &lt;matt@littleball.co.uk&gt;\n */\n</code></pre>\n\n<h3>Method</h3>\n\n<p>The method tag documents the name, parameters, and return values of the method.</p>\n\n<pre><code>/**\n * @method renamePerson\n * Changes the name of this person\n *\n * @param {String} newName The new name of the person\n *\n * @return {String} A message about the change of name\n */\n Person.prototype.renamePerson = function (newName) {\n    var oldName = this.name;\n    this.name = newName;\n    return oldName + ' has changed their name to ' + this.name;\n  };\n</code></pre>\n\n<h3>Ignore</h3>\n\n<p>If there are parts of your class that you do not want to be documented, you can add the ignore tag</p>\n\n<pre><code>/**\n * @ignore\n */\nPerson.prototype.hiddenFunction = function () {\n  this.doSomethingHidden();\n};\n</code></pre>\n\n<h3>Private</h3>\n\n<p>To make a function as private you can add the <code>@private</code> tag.</p>\n\n<pre><code>/**\n * Provite utility to return a private string\n * @private\n */\nvar privateFunction = function () {\n  return 'This is private';\n};\n\nmodule.exports = {\n  publicFunction: function () {\n    return 'This is public -&gt;' + privateFunction();\n  }\n};\n</code></pre>\n","title":"Documenting JavaScript"});